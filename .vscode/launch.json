{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Backend",
      "type": "node-terminal",
      "request": "launch",
      "command": "cd ${workspaceFolder}/backend && npm run dev"
    },
    {
      "name": "Run Frontend",
      "type": "node-terminal",
      "request": "launch",
      "command": "cd ${workspaceFolder}/frontend && npm run dev -- --host 0.0.0.0 --port 5173"
    }
  ]
}
